http {

    server {
        listen 0.0.0.0:80;
        server_name api.mercurywallet.io;

        location / {
            root /var/www/html;
            index index.html;
            try_files $uri $uri/ =404;
        }
    }
}
